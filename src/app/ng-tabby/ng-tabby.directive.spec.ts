import { NgTabbyDirective } from './ng-tabby.directive';

describe('NgTabbyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTabbyDirective();
    expect(directive).toBeTruthy();
  });
});
