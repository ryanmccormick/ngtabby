import { NgTabbySettings } from './ng-tabby-settings.model';

/**
 * NgTabby Default Settings.
 * @type {{tabindexStart: number; controlClassName: string; showErrorDebug: boolean}}
 */
export const NGTDEFAULTS: NgTabbySettings = {
  tabindexStart: 1,
  controlClassName: 'ngt-control',
  showErrorDebug: true
};
